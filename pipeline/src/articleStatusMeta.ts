// The status machine's shared metadata table. Lives in the CMS so the Payload
// collection can consume it; re-exported here the same way as `pricing.ts`.
export * from '../../cms/src/lib/articleStatusMeta'
